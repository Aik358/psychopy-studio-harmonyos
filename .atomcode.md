# PsychoPy-OH 鸿蒙移植项目指令

## 项目概要

PsychoPy Studio 2026.1.2 → HarmonyOS 6.1（麒麟 X90a）
技术路线：**Electron-OH**（Chromium 132 + 鸿蒙适配），嵌入 HAP 包
仓库：https://atomgit.com/A9iska/psychopy-oh（分支 v0.1.6）

## 架构层级

```
SvelteKit 前端 (contextBridge IPC)
  → Electron 窗口/文件/系统层 (WebSocket JSON-RPC Liaison)
    → uv + Python venv 运行时
      → psychopy-lib 实验引擎
```

## 关键目录

| 目录 | 用途 |
|------|------|
| `electron/libs/arm64-v8a/` | .so 原生库 |
| `electron/src/main/ets/` | ArkTS 入口 |
| `web_engine/src/main/ets/` | ArkTS 桥接层（adapter, jsbindings, ability） |
| `web_engine/.../electron/src/` | Electron 主进程源码（index.cjs, preload.js, git.js） |
| `web_engine/.../electron/dist/` | SvelteKit 前端构建产物 |
| `web_engine/.../app/.svelte-kit/` | SvelteKit 源码及构建缓存 |

## 开发规范

### ArkTS 语法规范
- 把 `.ets` 当作 **ArkTS** 处理，不是通用 TypeScript
- **禁止**使用 `any`、`unknown`（除非用户明确允许）
- **禁止**使用 `as` 类型断言
- **禁止**使用模板字符串（`` `${value}` ``）；用字符串拼接
- **禁止**使用解构声明、`delete`、`in`、`for...in`
- 对象字面量需要显式类型上下文
- 优先使用命名类、接口和显式 `implements`

### ArkTS 编译错误修复
| 错误类型 | 修复方案 |
|---------|---------|
| Notification 类型错误 | 转换为 `number` 类型 |
| Window 类型错误 | 使用回调模式调用 `getLastWindow` |
| @StorageLink 缺省值错误 | 添加 `= undefined` 或具体默认值 |
| 对象字面量接口错误 | 先定义接口再使用对象字面量 |
| 函数返回类型错误 | 添加显式返回类型注解 |
| 箭头函数转换错误 | 将 `function` 转为箭头函数 `=>` |
| catch 子句类型错误 | 删除类型注解或使用 `unknown` |

### 运行时崩溃诊断
当出现白屏/闪退/TypeError 时：
1. 获取崩溃锚点：`error_type`、`error_message`、`suspected_file`
2. 用 `hilog` 抓取设备日志（过滤 bundleName `com.huawei.ohos_electron`）
3. 定向读取疑似文件，做最小修改
4. 不凭空猜测，拿到证据再下手

### .so 文件处理
- Git LFS 管理，checkout 后需从 `.git/lfs/objects/` 复制真实二进制
- 验证：`file *.so` 应显示 `ELF 64-bit ... ARM aarch64`

### 前端构建（dist/）
- 不在 git 中跟踪，需从 `.svelte-kit/output/client/` 复制
- 预渲染页面在 `.svelte-kit/output/prerendered/pages/` 下

## 副本一致性规则（2026-07-16 增）

项目里以下文件存在多份副本，修改时**必须同步所有副本**：

| 文件 | 副本数 | 路径 |
|------|--------|------|
| `liaison_shim.py` | 3 | 项目根、`hap_inspect/.../electron/src/python/`、`web_engine/.../electron/src/python/` |
| `electron/src/python/lib/` 目录 | 2 | `hap_inspect/.../electron/src/python/lib/`、`web_engine/.../electron/src/python/lib/` |

**设备运行时用的是 hap_inspect 副本**（对应 `electron/src/python/lib` 路径）。历史教训：d4be095 只往 web_engine 副本里加了 json_tricks，漏了 hap_inspect，导致设备端报 `ModuleNotFoundError: No module named 'json_tricks'` → 链式引发 `logging is not defined` / Write/Run/Run in browser 全失效。本轮 ceb92e1 补齐。

## json_tricks 缺失的完整症状链（2026-07-16 增）

```
json_tricks 缺失（hap_inspect 副本 lib 目录）
  → psychopy/data/base.py:13 import 失败
  → psychopy.data 无法加载
  → psychopy/experiment/params.py:25 `from psychopy import data, logging` 失败
  → 'logging is not defined'（writeScript/getElementProfiles 都报这个）
  → psychopy.experiment 整个 import 铭断
  → getAllComponents / getAllStandaloneRoutines / getAllElements 全空
  → Write/Run .js、Run in browser、Run .py 全失效（底层都依赖 getElementProfiles）
```

修复：把 web_engine 副本里的 json_tricks 9 个文件复制到 hap_inspect 对应 lib 目录。部署需重新打包 HAP 并重装到设备。

## astunparse 缺失（2026-07-17 增，json_tricks 下一环）

`psychopy/experiment/py2js.py:15` `import astunparse`，缺则整条 experiment import 链断（同 json_tricks 症状链）。astunparse 是纯 Python（4 文件），依赖 six（设备已有）+ wheel。已从本机 `~/.local/lib/python3.12/site-packages/astunparse/` v1.6.3 复制到两份 lib 副本。

**全量缺包扫描结论**（遍历 psychopy 关键链 import，对照设备 site-packages + 项目 lib + mock 列表）：
- 关键链唯一缺：astunparse（本轮补）
- 其他 MISS（soundfile/h5py/tables/questplus/psychtoolbox/psychxr/moviepy/meshpy/metapensiero/Image/cPickle）均在非关键路径或已被 mock，不阻塞 getAllComponents/writeScript

## XCollie `/sys/power/last_sr` 日志（2026-07-16 增，无害忽略）

设备 hilog 持续每 ~3 秒报 `Failed to open file: /sys/power/last_sr`，来源是 HarmonyOS 内核层 XCollie 系统看门狗轮询电源状态时读不到该 sysfs 节点。纯系统日志，与 psychopy-oh / Electron 应用代码无关，无害，无法从应用层修复，**直接忽略**。